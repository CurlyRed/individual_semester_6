apiVersion: apps/v1
kind: Deployment
metadata:
  name: wcd-projector-service
  namespace: wcd-platform
  labels:
    app: projector-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: projector-service
  template:
    metadata:
      labels:
        app: projector-service
    spec:
      containers:
      - name: projector-service
        image: ghcr.io/curlyred/individual_semester_6/projector-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8082
        env:
        - name: SERVER_PORT
          value: "8082"
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        - name: KAFKA_CONSUMER_GROUP_ID
          value: "projector-service-group"
        - name: JAVA_OPTS
          value: "-Xmx128m -Xms64m"
        envFrom:
        - configMapRef:
            name: wcd-app-config
        - secretRef:
            name: wcd-app-secrets
        resources:
          requests:
            memory: "192Mi"
            cpu: "10m"
          limits:
            memory: "384Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: http
          initialDelaySeconds: 120
          periodSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: http
          initialDelaySeconds: 90
          periodSeconds: 10
          failureThreshold: 5
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: wcd-projector-service
  namespace: wcd-platform
  labels:
    app: projector-service
spec:
  type: ClusterIP
  selector:
    app: projector-service
  ports:
  - name: http
    port: 8082
    targetPort: http
