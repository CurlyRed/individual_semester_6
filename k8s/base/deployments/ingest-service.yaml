apiVersion: apps/v1
kind: Deployment
metadata:
  name: wcd-ingest-service
  namespace: wcd-platform
  labels:
    app: ingest-service
    app.kubernetes.io/name: ingest-service
    app.kubernetes.io/part-of: wcd-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingest-service
  template:
    metadata:
      labels:
        app: ingest-service
        app.kubernetes.io/name: ingest-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: ingest-service
        image: ghcr.io/curlyred/individual_semester_6/ingest-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8081
          protocol: TCP
        env:
        - name: SERVER_PORT
          value: "8081"
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        - name: JAVA_OPTS
          value: "-Xmx256m -Xms128m"
        envFrom:
        - configMapRef:
            name: wcd-app-config
        - secretRef:
            name: wcd-app-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: http
          initialDelaySeconds: 90
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: http
          initialDelaySeconds: 60
          periodSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: wcd-ingest-service
  namespace: wcd-platform
  labels:
    app: ingest-service
spec:
  type: ClusterIP
  selector:
    app: ingest-service
  ports:
  - name: http
    port: 8081
    targetPort: http
    protocol: TCP
